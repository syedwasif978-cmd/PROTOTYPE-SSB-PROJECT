# Backend

Minimal Node.js API stub for the AI Excel chatbot prototype.

## Scripts

- `npm run dev` - run the backend with hot reload
- `npm run build` - compile TypeScript to `dist/`
- `npm run start` - run the compiled server
