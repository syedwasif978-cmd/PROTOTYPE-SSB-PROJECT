# Frontend

Next.js prototype UI for the AI Excel chatbot.

## Scripts

- `npm run dev` - start the frontend locally
- `npm run build` - build for production
- `npm run lint` - lint the frontend

## Deployment

This folder is Vercel-ready. Use `frontend/vercel.json` when deploying the UI separately.
